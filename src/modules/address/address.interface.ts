export interface IAddressInfo {
  fullName: string;
  isDefault?: boolean;
  province: string;
  city: string;
  postalAddress: string;
  buildingNumber: number;
  unit?: number;
  postalCode: string;
}
